# omo-switcher Flutter client

Flutter macOS client for `omo-switcher`. It talks to the existing Node server over HTTP and stores private settings/config item cache locally.

```bash
flutter pub get
flutter test
flutter run -d macos
```
